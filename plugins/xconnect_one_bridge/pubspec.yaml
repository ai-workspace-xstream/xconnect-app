name: xconnect_one_bridge
description: Optional local bridge for composing XConnect APP with XConnect One.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
