interface:
  display_name: "XConnect Functional Baseline"
  short_description: "Verify macOS tunnel, proxy, UI, and external browsing health"
  default_prompt: "Use $xconnect-functional-test-baseline to verify XConnect build health, System VPN tunnel mode, proxy mode, UI start-stop-reconnect-mode-switch behavior, routing, data-plane checks, and external browsing benchmarks on macOS."
